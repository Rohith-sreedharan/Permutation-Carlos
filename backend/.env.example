# Copy this to .env and fill in the values
ODDS_API_KEY=your_api_key_here
ODDS_BASE_URL=https://api.the-odds-api.com/v4
MONGO_URI=mongodb://localhost:27017
DATABASE_NAME=beatvegas

# API-SPORTS Configuration (for injury data)
# Sign up at https://api-sports.io/ for free tier (100 requests/day)
APISPORTS_KEY=your_apisports_key_here

# CORS configuration (backend/.env)
# Comma-separated origins — list the frontend origins exactly (include port)
# CORS_ALLOW_ORIGINS=http://localhost:5173,http://127.0.0.1:3000

# # Allow credentials only if you use cookies or fetch(..., { credentials: 'include' })
# CORS_ALLOW_CREDENTIALS=true